# Apex Moto Supply 项目规则

## 适用范围与技术栈

- 本项目是静态多语言官网：HTML、CSS、原生 JavaScript。
- 产品详情页由 Node.js 脚本根据 JSON、模板和共享翻译生成。
- 后端入口是 Cloudflare Worker，使用 D1、静态资源绑定及 SMTP 环境变量。
- 浏览器端 Excel 导出使用仓库内置的 JSZip；不得擅自替换或删除。

## 修改前必读

1. 根目录 `CODEX.md`。
2. 目标模块的 `CODEX.md`，前端任务必须读 `frontend/CODEX.md`。
3. 产品详情任务还必须读 `frontend/product-detail/README.md`、`schema.json`。
4. 继续任务前读 `CODEX_HANDOFF.md` 和 `TASKS.md`。

## 代码与文件规则

- 只修改需求所属的最小模块，禁止无关重构、全局格式化或批量重写。
- `deploy/` 是生成目录，不得手工维护；仅在明确要求发布准备时运行生成命令。
- 车型 ID、JSON 和页面文件统一使用小写 kebab-case：`<model-id>.json`、`<model-id>.html`。
- 车型资产使用车型前缀和版本后缀；AI/整改图创建新版本，保留官方原图和已验收旧版本。
- 静态 HTML 保持 UTF-8；中文实体和现有 Unicode 转义策略不得被批量改写。
- 不得覆盖、回退或删除未确认归属的现有改动和未跟踪资产。

## 不允许破坏的内容

- 公开 URL、顶部导航、语言切换、产品世界菜单、图库切换、标签页、灯箱、询盘入口和 Excel 下载。
- `/api/*` 契约、D1 字段、SMTP、管理后台认证及 `emailStatus` 行为。
- 七语言：`en`、`zh-CN`、`zh-TW`、`ru`、`ar`、`es`、`pt`；阿拉伯语 RTL 不得改变图片区顺序。
- 产品详情固定五图顺序：正面、左前 3/4、左侧、左后 3/4、后面；必须完整显示车辆且无黑边。
- 每款车型恰好四项统计、四项卖点、四项核心亮点；参数不得猜测。
- 右侧 Panel 保持 SJ300 体系：完整左前 3/4、暗黑工业环境、局部红色轮廓光、烟雾和湿地反射，禁止整幅发红。
- 产品世界悬停预览保持无字底图、HTML 文字层、顶部安全区和完整车型，不得把其他车型结构混入。

## 数据来源优先级

1. Excel 车型清单决定车型范围。
2. 官网存在且车型身份一致时，参数、文字和真实部件图以官网为准。
3. 官网缺项时使用 Excel；官网无该车型时完全以 Excel 为准。
4. 官网和 Excel 都无所需图片时才允许 AI 生成，且必须以真实车型图锁定身份。
5. 不覆盖原始 Excel；核对结果写入更新副本。

## 必须执行的验证

- 修改 JS/MJS/Worker 后运行对应的 `node --check`。
- 修改车型 JSON、模板或生成器后运行目标车型构建；最终批次运行隔离的 `--all` 构建。
- 运行 `git diff --check`。
- UI 修改至少检查 375、768、1440；共享布局最终抽查 1920、2560。
- 验证 HTTP 和 `file:///`、七语言/RTL、控制台、图片 404、横向滚动、裁切、重叠和键盘焦点。
- 最多同时运行三个高负载任务；不得重复启动构建、浏览器、服务器或图片生成。

## Git 与发布

- 未经明确要求不得部署、暂存、提交、推送、回退或清理用户文件。
- 密钥、SMTP 凭据和 `ADMIN_KEY` 只能放环境变量，绝不写入仓库。
